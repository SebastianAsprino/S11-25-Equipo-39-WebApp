---
import LayoutForm from "@/layouts/LayoutForm.astro";
---

<LayoutForm>
  <main class="min-h-screen w-full bg-base-200 flex items-center justify-center px-4 py-8">
    <div
      class="w-full 
             sm:card sm:bg-base-100 sm:border sm:border-base-300 sm:rounded-xl sm:shadow-md
             max-w-full sm:max-w-xs md:max-w-sm lg:max-w-md"
    >
      <div class="flex flex-col gap-3 px-4 py-6
                  sm:card-body sm:gap-4 sm:px-6 sm:py-8
                  md:px-8 md:py-10">

        <h1 class="text-xl sm:text-2xl md:text-2xl lg:text-3xl font-bold text-center leading-tight">
          Restablecer tu Contraseña
        </h1>

        <p class="text-sm sm:text-base md:text-sm lg:text-base text-center text-base-content/70 leading-relaxed">
          Ingresa el correo electrónico asociado a tu cuenta y te enviaremos un enlace para restablecer tu contraseña.
        </p>

        <form class="flex flex-col gap-3 mt-1 w-full">
          <div class="form-control w-full">
            <label class="label">
              <span class="label-text text-sm sm:text-base md:text-sm font-semibold">
                Correo Electrónico
              </span>
            </label>
            <input
              type="email"
              placeholder="ejemplo@correo.com"
              class="input input-bordered w-full h-10 sm:h-10 text-sm sm:text-sm md:text-xs lg:text-sm"
              required
            />
          </div>

          <button
            type="submit"
            class="btn btn-primary w-full h-10 sm:h-10 text-sm sm:text-sm md:text-xs lg:text-sm font-semibold"
          >
            Enviar Enlace de Restablecimiento
          </button>
        </form>

        <div class="text-center mt-2">
          <a
            href="/login"
            class="link link-hover text-xs sm:text-sm md:text-xs"
          >
            ¿Recuerdas tu contraseña? Volver a Inicio de Sesión
          </a>
        </div>

      </div>
    </div>
  </main>
</LayoutForm>
